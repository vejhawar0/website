### Requirements
* Need to have node installed on your device

### Building
Build using `./make.sh`

### How to run local server
Run `npx http-server -p 8080`

While running, navigate to `http://localhost:8080`
